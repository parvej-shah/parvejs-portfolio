# Sections (Section CMS) — Purpose

Phase 2. Makes the homepage's static sections (hero, services, stats, about, skills,
testimonials, CTA, contact, social links, SEO) editable from the admin panel, instead of
hardcoded in their respective `components/*.jsx` files.

Deferred until Projects + Blog (Phase 1) ship, to keep the two-agent parallel work low-conflict
early on. Do not start this feature until `TASKS.md` Wave 3 is reached.
